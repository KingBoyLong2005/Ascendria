using UnityEngine;

public class InventoryDebugAdder : MonoBehaviour
{
    public ItemData[] sampleItems;

    // void Update()
    // {
    //     if (Input.GetKeyDown(KeyCode.K))
    //     {
    //         if (sampleItems != null && sampleItems.Length > 0)
    //         {
    //             var pick = sampleItems[Random.Range(0, sampleItems.Length)];
    //             InventoryManager.Instance?.AddItem(pick);
    //         }
    //     }
    // }
}
